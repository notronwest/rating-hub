# Cloudflare Pages headers. Hashed Vite assets can be cached forever; the
# HTML shell must not be, otherwise deploys don't show up for returning users.

/assets/*
  Cache-Control: public, max-age=31536000, immutable

/index.html
  Cache-Control: public, max-age=0, must-revalidate
